@{
    ///code inside this file executes before any view is rendered. 
    ///so puuting the layout (masterpage) here makes sure every file will use this layout specified
    ///In every subfolder under the "views" folder we can put a different _ViewStart.cshtml
    Layout = "~/Views/Shared/_Layout.cshtml";
}